@{
    ViewData["Title"] = "Dashboard";
    ViewData["ActiveSection"] = "dashboard";
}

<section class="admin-section active">
    <p class="hint">Platform KPIs, provider health, and the request queue land here in a follow-up pass.</p>
    <p>
        <a href="/Admin/Channels"><strong>Channels</strong></a> is wired up first since it carries this phase's
        requested change: the per-channel headline stat is now live RPM instead of raw concurrency.
    </p>
</section>
