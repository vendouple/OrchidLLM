/**
 * /api/v1/audio/transcriptions — OpenAI-compatible Audio Transcriptions (v1 namespace)
 * Mirrors /api/audio/transcriptions under the versioned v1 path.
 */
import handler from '../../audio/transcriptions.js';
export default handler;
