/**
 * /api/v1/audio/speech — OpenAI-compatible Speech/TTS (v1 namespace)
 * Mirrors /api/audio/speech under the versioned v1 path.
 */
import handler from '../../audio/speech.js';
export default handler;
