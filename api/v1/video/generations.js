/**
 * /api/v1/video/generations — Orchid video generation extension (v1 namespace)
 * Mirrors /api/video/generations under the versioned v1 path.
 */
import handler from '../../video/generations.js';
export default handler;
