/**
 * /api/v1/images/generations — OpenAI-compatible Image Generations (v1 namespace)
 * Mirrors /api/images/generations under the versioned v1 path.
 */
import handler from '../../images/generations.js';
export default handler;
